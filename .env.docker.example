# Copy this file to .env.docker and fill in secret values before running docker compose.

# Backend JWT Configuration
JWT_SECRET=replace-with-your-secret
JWT_EXPIRES_IN=7d

# Admin credentials for local development
ADMIN_USER=admin
ADMIN_PASS=replace-with-strong-password

# Database and Redis URLs are configured in docker-compose.yml
